# V0.3 实现提示词 — 发给Codex

---

## 目标

在 /Users/lemonsea/Desktop/mas/kernel/ 实现V0.3：BusinessLine框架MVP + 基础可扩展性

---

## 背景

Kernel是一个OPC Multi-Agent操作系统。

当前状态：V0.2完成，58个测试通过，GitHub已同步。

V0.3目标：实现BusinessLine核心 + 基础可扩展性。

---

## 核心架构决策

**BusinessLine是第一等公民：**

- 不是tenant_id字段，是独立的实体
- 有完整生命周期：CREATE/ACTIVE/PAUSED/ARCHIVED/DELETED
- 业务线之间完全隔离
- Shared层资源可跨业务线复用

**高可扩展性：**

- 8个插件扩展点
- 新增功能通过注册机制，不是修改核心代码

---

## 创新要求（每个功能必须思考）

### 1. 不要照搬市面框架

不要想"CrewAI/LangGraph怎么做的"。

要问自己：OPC场景有没有更简单的交互方式？

### 2. 让用户少操作一步

能不能零配置？能不能默认值就是对的？

### 3. 防止出错的兜底机制

错误是预期的，不是意外。Agent可能误判，Policy要有兜底。

### 4. 能不能自我进化

能不能从历史中学到规律？成功的经验能不能复用？

### 5. 和其他模块配合

不是实现一个孤立的功能，是和其他模块（Policy/Evaluation/HumanGate/Context）配合。

---

## 具体任务

### 阶段1：BusinessLine模型

创建 kernel/businessline/models.py：

```python
class BusinessLineStatus(str, Enum):
    ACTIVE = "active"
    PAUSED = "paused"
    ARCHIVED = "archived"
    DELETED = "deleted"

class BusinessLine(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    name: str
    status: BusinessLineStatus = BusinessLineStatus.ACTIVE
    config: BusinessLineConfig = BusinessLineConfig()
    created_at: float = Field(default_factory=time.time)
    last_active_at: float = Field(default_factory=time.time)

class BusinessLineConfig(BaseModel):
    evaluation_rules: list[str] = []
    resource_limits: ResourceLimits = ResourceLimits()

class ResourceLimits(BaseModel):
    max_concurrent_runs: int = 10
    max_agents: int = 50
```

### 阶段2：BusinessLineRegistry

创建 kernel/businessline/registry.py：

```python
class BusinessLineRegistry:
    """业务线注册表"""
    
    def __init__(self, storage: StorageBackend):
        self._storage = storage
        self._cache: dict[str, BusinessLine] = {}
    
    def create(self, name: str, config: BusinessLineConfig = None) -> BusinessLine:
        """创建业务线"""
    
    def get(self, bl_id: str) -> BusinessLine:
        """获取业务线"""
    
    def list(self, status: BusinessLineStatus = None) -> list[BusinessLine]:
        """列出业务线"""
    
    def update_status(self, bl_id: str, status: BusinessLineStatus) -> BusinessLine:
        """更新状态"""
    
    def delete(self, bl_id: str):
        """删除业务线"""
```

### 阶段3：Kernel集成BusinessLine

修改 kernel/kernel.py：

```python
class Kernel:
    def __init__(self):
        self.business_lines = BusinessLineRegistry(self.store)
    
    def create_business_line(self, name: str, config: BusinessLineConfig = None) -> BusinessLine:
        return self.business_lines.create(name, config)
    
    def get_business_line(self, bl_id: str) -> BusinessLine:
        return self.business_lines.get(bl_id)
    
    def list_business_lines(self, status: BusinessLineStatus = None) -> list[BusinessLine]:
        return self.business_lines.list(status)
```

### 阶段4：资源按BusinessLine隔离

所有模型的get方法加business_line_id过滤：

```python
# Agent Registry
def get(self, agent_id: str, business_line_id: str = None) -> Agent:
    key = f"agent:{business_line_id}:{agent_id}" if business_line_id else f"agent:{agent_id}"
    return self._storage.load(key)

# Context Manager
def get_context(self, agent_name: str, business_line_id: str) -> dict:
    # 按business_line_id隔离

# Runs
def list_runs(self, business_line_id: str) -> list[Run]:
    # 按business_line_id过滤
```

### 阶段5：插件机制基础

创建 kernel/plugins/registry.py：

```python
class PluginRegistry:
    """插件注册表"""
    
    def __init__(self):
        self._agents = {}
        self._tools = {}
        self._evaluators = {}
        self._providers = {}
    
    def register_agent(self, name: str, agent_class: type[BaseAgent]):
        self._agents[name] = agent_class
    
    def register_tool(self, name: str, tool_class: type[BaseTool]):
        self._tools[name] = tool_class
    
    def register_evaluator(self, name: str, evaluator_class: type[BaseEvaluator]):
        self._evaluators[name] = evaluator_class
    
    def get_agent(self, name: str) -> type[BaseAgent]:
        return self._agents.get(name)
    
    def get_tool(self, name: str) -> type[BaseTool]:
        return self._tools.get(name)
    
    def get_evaluator(self, name: str) -> type[BaseEvaluator]:
        return self._evaluators.get(name)
```

### 阶段6：成本控制（Token计数）

创建 kernel/observability/cost_tracker.py：

```python
class CostTracker:
    """成本追踪"""
    
    def __init__(self):
        self._counters: dict[str, TokenCounter] = {}
    
    def track(self, provider: str, input_tokens: int, output_tokens: int):
        """记录Token消耗"""
    
    def get_cost(self, provider: str = None) -> CostBreakdown:
        """获取成本"""
    
    def reset(self, provider: str = None):
        """重置计数器"""

class TokenCounter:
    input_tokens: int = 0
    output_tokens: int = 0
    
    @property
    def total_tokens(self) -> int:
        return self.input_tokens + self.output_tokens
```

集成到LLM Provider调用：

```python
# 在MiniMaxProvider.chat()中
async def chat(self, messages, **kwargs):
    response = await self._call_api(messages)
    cost_tracker.track("minimax", response.usage.input_tokens, response.usage.output_tokens)
    return response
```

---

## 创新检查清单

每实现一个功能，回答：

1. 这个功能的OPC交互方式是什么？比市面框架简单在哪里？
2. 能不能让用户零配置？
3. 出错了怎么处理？
4. 能不能从历史中学到东西？
5. 和Policy/Evaluation/HumanGate怎么配合？

---

## 验证命令

```bash
cd /Users/lemonsea/Desktop/mas/kernel

# 基础验证
python -m unittest discover -s tests -v

# BusinessLine测试
python -c "
from kernel.businessline import BusinessLineRegistry
from kernel import Kernel

k = Kernel()

# 创建两个业务线
bl1 = k.create_business_line('业务A')
bl2 = k.create_business_line('业务B')

print(f'BL1: {bl1.id}')
print(f'BL2: {bl2.id}')

# 验证隔离
assert bl1.id != bl2.id
print('BusinessLine创建成功')

# 验证状态更新
k.business_lines.update_status(bl1.id, 'paused')
print('状态更新成功')
"

# 插件注册测试
python -c "
from kernel.plugins import PluginRegistry

reg = PluginRegistry()
reg.register_agent('test_agent', object)
assert reg.get_agent('test_agent') is not None
print('插件注册成功')
"

# 成本追踪测试
python -c "
from kernel.observability import CostTracker

ct = CostTracker()
ct.track('minimax', 1000, 500)
cost = ct.get_cost('minimax')
print(f'Tokens: {cost.total_tokens}')
print('成本追踪成功')
"
```

---

## 提交要求

```bash
git add -A
git commit -m "V0.3: BusinessLine框架MVP

- BusinessLine模型 + 生命周期
- BusinessLineRegistry
- 资源按BL隔离
- 插件注册机制
- 成本追踪
- 58->65+测试"
git push
```

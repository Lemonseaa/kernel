[project]
name = "kernel"
version = "1.0.0"
description = "Agent Workflow Kernel for a general multi-agent operating system."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
